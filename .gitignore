# --- Oyun dosyalari (cok buyuk + yarisma materyali; repoya KONMAZ) ---
Drones of War Teknofest/

# --- Buyuk model ---
# pose modeli 123 MB > GitHub 100 MB siniri (push REDDEDILIR). Aktif model best.pt (50 MB).
bestpose.pt
models/bestpose.pt
# ham Colab ciktisi 126 MB (optimizer dahil) > 100 MB siniri. Calisan kopyasi
# models/talon_pose.pt optimizer'i temizlenmis halde (42 MB) REPODA.
talon_yolo11m_pose_2_best.pt

# --- Calisma zamani ciktilari (log/json/png hepsi veri/ altinda uretilir) ---
veri/
kiyas_log.csv
gorsel_log.csv
# duz-yapi calistirmalarindan kok dizine dusen loglar (analiz_ucus.py / tani_irtifa.py)
ucus_log*.csv
ucus_metrikler.csv
tani_log.csv
*.log

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# --- Isletim sistemi / editor ---
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Yanlislikla olusan cift uzantili kopya (dogrusu: omer_filtre.py)
omer_filtre.py.py

# --- TensorRT motorlari: MAKINEYE OZEL, repoya KONMAZ ---
# .engine dosyasi GPU mimarisine + surucu + TensorRT surumune gore derlenir.
# Baska makineden kopyalanan ya yuklenmez ya yanlis calisir.
# Her makinede uretilir:  python arac/motor_kur.py   (bkz. FPS_VE_MODEL.md)
models/*.engine
*.engine

# ══ 2026-08-24: DoW sistemi push edilirken eklendi ══════════════════════
# Asil kod ~5 MB; asagidakiler 940 MB tutuyor ve hicbiri sistemi calistirmak
# icin gerekli degil. Gereken model (talon_v5.pt) ZATEN repoda izleniyor.

# --- Arsiv/yedek kopyalari (kod degil, eski surumlerin tam kopyalari) ---
yedek/
kopru/gazebo_kaynak_ONCEKI*/
PAYLASIM_MODELLER_*/

# --- Claude Code oturum verisi (transkript + worktree; paylasilmaz) ---
.claude/

# --- ONNX disa aktarimlari: .pt'den yeniden uretilir, 108 MB tutuyor ---
*.onnx

# --- Ek model varyantlari (aktif sistem v5 kullaniyor) ---
models/talon_v2.pt
models/talon_v3.pt
models/talon_pose_v2.pt
models/best_yolo11s_*.pt
models/best_talon_v1*.pt
models/*.yedek_*
yolo26n.pt

# --- Calisma zamani ciktilari ---
sunucu_cikti.txt
sunucu_hata.txt
ayrik_log/
.gudum_kipi

# arayuz calisma logu (0.7 MB, her kosuda yeniden uretilir)
arayuz_log.txt
